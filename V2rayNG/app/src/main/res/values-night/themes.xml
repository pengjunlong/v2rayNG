<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppThemeDayNight" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
        <!-- TV: AlertDialog button focus highlight (same as light) -->
        <item name="buttonBarButtonStyle">@style/TvDialogButtonStyle</item>
    </style>

    <style name="AppThemeDayNight.NoActionBar" parent="AppThemeDayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- TV: Toolbar icon / overflow-menu button focus highlight -->
        <item name="actionBarItemBackground">@drawable/bg_toolbar_item_focus</item>
    </style>

</resources>