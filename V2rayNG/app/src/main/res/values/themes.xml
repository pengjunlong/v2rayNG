<resources>

    <style name="AppThemeDayNight" parent="Theme.AppCompat.DayNight">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="colorControlNormal">@color/colorAccent</item>
        <!-- TV: AlertDialog button focus highlight -->
        <item name="buttonBarButtonStyle">@style/TvDialogButtonStyle</item>
    </style>

    <!-- TV: AlertDialog / ButtonBar button with focus highlight -->
    <style name="TvDialogButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:background">@drawable/bg_dialog_button_focus</item>
        <item name="android:textColor">@color/color_fab_active</item>
        <item name="android:minWidth">64dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
    </style>

    <style name="AppThemeDayNight.NoActionBar" parent="AppThemeDayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- TV: Toolbar action items (search, play/stop, add…) focus highlight -->
        <item name="actionBarItemBackground">@drawable/bg_toolbar_item_focus</item>
        <!-- TV: 右侧三点溢出按钮焦点样式 -->
        <item name="actionOverflowButtonStyle">@style/TvOverflowButtonStyle</item>
        <!-- TV: 左侧汉堡/返回按钮焦点样式 -->
        <item name="toolbarNavigationButtonStyle">@style/TvToolbarNavButtonStyle</item>
        <!-- TV: 溢出菜单弹出层条目焦点样式 -->
        <item name="popupMenuStyle">@style/TvPopupMenuStyle</item>
        <item name="listPopupWindowStyle">@style/TvPopupMenuStyle</item>
    </style>

    <!-- TV: 右侧三点溢出按钮 -->
    <style name="TvOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:background">@drawable/bg_toolbar_item_focus</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <!-- TV: 左侧汉堡 / 返回按钮 -->
    <style name="TvToolbarNavButtonStyle" parent="Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:background">@drawable/bg_toolbar_item_focus</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <!-- TV: 溢出菜单弹出层，条目背景使用 bg_popup_menu_item_focus -->
    <style name="TvPopupMenuStyle" parent="Widget.AppCompat.PopupMenu">
        <item name="android:itemBackground">@drawable/bg_popup_menu_item_focus</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.NoActionBar.Translucent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="BrandedSwitch" parent="AppTheme">
        <item name="colorAccent">@color/color_fab_active</item>
    </style>
</resources>
